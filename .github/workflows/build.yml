name: Build

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build and analyze
    runs-on: ubuntu-latest
    env:
      BUILD_WRAPPER_OUT_DIR: build_wrapper_output_directory # Directory where build-wrapper output will be placed
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - name: Install Build Wrapper
        uses: SonarSource/sonarqube-scan-action/install-build-wrapper@v7
        env:
          SONAR_HOST_URL: ${{secrets.SONAR_HOST_URL}}
      - name: Run Build Wrapper
        run: |
          build-wrapper-linux-x86-64 --out-dir ${{ env.BUILD_WRAPPER_OUT_DIR }} g++ src/helloworld.cpp -o helloworld
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v7
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{secrets.SONAR_HOST_URL}}
        with:
          args: >
            -Dsonar.cfamily.compile-commands=${{ env.BUILD_WRAPPER_OUT_DIR }}/compile_commands.json



# name: "SONAR GENERIC"
# on: push
# jobs:
#   scan:
#     #uses: DevSecOps4SG/Templates/.github/workflows/sonar_generic.yml@dev
#     uses: 075-O2/zftm_u01_2.5.6_paymentprop/.github/workflows/sonar_generic.yml@development
#     with:
#       SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }}
#       SONAR_PROJECT_KEY: ${{ vars.SONAR_PROJECT_KEY }}
#       SONAR_SOURCES: "./webapp"
      
    # secrets:
    #   SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
